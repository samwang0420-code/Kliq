# === Kliq 环境变量 ===
#
# 复制本文件为 .env 并填入真实值。.env 已在 .gitignore 中，不会被提交。
#
# 重要提示:
# - 客户端变量（VITE_ 前缀）由 Vite 在**构建期**注入并打进前端 bundle，
#   因此只能放公开、非敏感的值（站点地址、结算页地址）。
# - 服务端密钥（LEMON_SQUEEZY_*）只配在 Cloudflare Pages 后台，
#   **绝不要**写成 VITE_ 前缀，否则会随安装包分发给每一位用户。
# - 转录 / 字幕走打包在应用内的本地 whisper 原生运行时，不需要任何云端 API key。

# ===== Lemon Squeezy 支付（Cloudflare Pages 运行时变量）=====
# 供 functions/api/license-validate.ts 调用 Lemon Squeezy Validate API 校验许可证。
# 配置位置: Cloudflare Pages → Settings → Environment variables
# LEMON_SQUEEZY_STORE=
# LEMON_SQUEEZY_API_KEY=
# LEMON_SQUEEZY_PRODUCT_ID=

# ===== 客户端收费闸口（构建期注入，以 VITE_ 开头才会进前端 bundle）=====
# 可选: 你自己的站点地址。配置后启用在线校验 + 退款页链接。
#   在线校验端点 = ${VITE_KLQ_SITE_URL}/api/license-validate
#   未配置 → 跳过在线校验，激活走离线格式校验（离线激活仍可用）
# VITE_KLQ_SITE_URL=https://example.com
#
# 可选: 你的 Lemon Squeezy 结算页 URL。未配置 → 个人中心的购买按钮禁用并提示。
# VITE_KLQ_CHECKOUT_URL=https://your-store.lemonsqueezy.com/checkout/buy/xxxx

# ===== 调试 =====
# 可选: 启用调试日志
# DEBUG=kliq:*
